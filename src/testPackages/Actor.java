package testPackages;


class Actor {
    String name;

    // Constructor
    Actor(String name) {
        this.name = name;
    }

    // Method to display actor details
    void display() {
        System.out.println("Actor: " + name);
    }
}
